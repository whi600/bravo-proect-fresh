services:
  vue:
    build:
      context: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - vue_node_modules:/app/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

volumes:
  vue_node_modules:
